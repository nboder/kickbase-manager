import { TotalStateDataProvider } from './total-state-data-provider';
describe('TotalStateDataProvider', () => {
  it('should create an instance', () => {
    expect(new TotalStateDataProvider()).toBeTruthy();
  });
});
