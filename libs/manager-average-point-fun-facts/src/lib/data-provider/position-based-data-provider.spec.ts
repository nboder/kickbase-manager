import { PositionBasedDataProvider } from './position-based-data-provider';

describe('PositionBasedDataProvider', () => {
  it('should create an instance', () => {
    expect(new PositionBasedDataProvider()).toBeTruthy();
  });
});
