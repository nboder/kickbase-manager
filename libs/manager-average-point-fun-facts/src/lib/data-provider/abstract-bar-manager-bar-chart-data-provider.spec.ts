import { AbstractBarManagerBarChartDataProvider } from './abstract-bar-manager-bar-chart-data-provider';

describe('AbstractBarManagerBarChartDataProvider', () => {
  it('should create an instance', () => {
    expect(new AbstractBarManagerBarChartDataProvider()).toBeTruthy();
  });
});
