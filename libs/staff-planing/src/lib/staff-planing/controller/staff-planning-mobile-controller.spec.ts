import { StaffPlanningMobileController } from './staff-planning-mobile-controller';

describe('StaffPlanningMobileController', () => {
  it('should create an instance', () => {
    expect(new StaffPlanningMobileController()).toBeTruthy();
  });
});
