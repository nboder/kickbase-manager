import { CompetitionTableEntry } from './competition-table-entry';
describe('CompetitionTableEntry', () => {
  it('should create an instance', () => {
    expect(new CompetitionTableEntry()).toBeTruthy();
  });
});
